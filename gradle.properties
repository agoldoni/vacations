# Impostazioni JVM per il daemon Gradle
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# AndroidX obbligatorio con AGP recenti
android.useAndroidX=true

# R class non transitive: build più veloci, riferimenti espliciti
android.nonTransitiveRClass=true

kotlin.code.style=official
